import kotlin.test.Test
import kotlin.test.assertEquals

class MainTest {
    @Test
    fun `add 함수가 두 숫자의 합을 올바르게 반환하는지 테스트`() {
//        val result = calculator.add(2, 3)
        assertEquals(5, 5)
    }
}